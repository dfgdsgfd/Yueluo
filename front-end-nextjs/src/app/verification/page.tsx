import { VerificationApplyPage } from "@/components/settings/verification-apply-page";

export const metadata = {
  title: "Verification",
};

export default function VerificationRoute() {
  return <VerificationApplyPage />;
}
